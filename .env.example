# StressKit reads process environment only; it does not load this file.
# Keep values empty in version control. Use a shell or secret manager at runtime.
OPENROUTER_API_KEY=

# Reserved for separately authorized Hugging Face artifact retrieval.
# Never pass this credential into `stresskit audit opinion` or audit workers.
HF_TOKEN=
